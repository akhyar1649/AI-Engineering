[project]
name = "kirimin-data-platform"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
  "pandas==3.0.3",
  "fastapi==0.141.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
